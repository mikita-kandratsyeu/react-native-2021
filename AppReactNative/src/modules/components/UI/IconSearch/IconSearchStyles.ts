import { StyleSheet } from 'react-native';
import { defaultStyles } from '../../../../constans';

const styles = StyleSheet.create({
  container: {
    marginLeft: defaultStyles.margin.medium,
    marginRight: defaultStyles.margin.medium,
    alignSelf: 'center',
  },
});

export default styles;
